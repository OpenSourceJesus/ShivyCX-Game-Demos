/* mbos.h -- compatibility shim so the files lifted verbatim from ShivyC's
 * examples/rpython2c/mbos (vbe.c, libmini.c, rpy/rt_freestanding.c) compile
 * in this demo without edits: they `#include "mbos.h"`, we provide kernel.h. */
#include "kernel.h"
